#include<stdio.h>
//ton determine no. is divisible by 97 or not
int main(){

    int n;
      printf("enter the number\n");
         scanf("%d" ,&n);
      printf("the divisibility tests returns %d", n%97);
    return 0;
}